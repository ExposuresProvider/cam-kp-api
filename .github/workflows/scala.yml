name: Scala CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Set up JDK 11
      uses: actions/setup-java@v1
      with:
        java-version: 11
    - name: Run tests
      env:
        CAM_KP_LOG_LEVE: "INFO"
        CAM_KP_SPARQL_ENDPOINT: "https://cam-kp-dev-sparql.apps.renci.org/blazegraph/sparql"
      run: sbt test
