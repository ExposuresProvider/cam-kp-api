{
  version = "0.3-pre1"
  host = 0.0.0.0
  port = 8080
  port = ${?PORT}
  trapi-version = "1.3.0"
  trapi-version = ${?TRAPI_VERSION}
  biolink-version = "v3.1.2"
  location = "http://localhost:8080"
  location = ${?LOCATION}
  sparql-endpoint = "https://cam-kp-sparql.apps.renci.org/sparql"
  sparql-endpoint = ${?SPARQL_ENDPOINT}
  maturity = "development"
  maturity = ${?MATURITY}
}
