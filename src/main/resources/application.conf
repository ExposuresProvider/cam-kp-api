{
  host = 0.0.0.0
  port = 8080
  port = ${?PORT}
  trapi-version = "1.2.0"
  trapi-version = ${?TRAPI_VERSION}
  location = "http://localhost:8080"
  location = ${?LOCATION}
  maturity = "development"
  maturity = ${?MATURITY}
}
