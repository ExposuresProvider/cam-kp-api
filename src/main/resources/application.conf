{
  host = 0.0.0.0
  port = 8080
  port = ${?PORT}
  trapi-version = "1.2.0"
  trapi-version = ${?TRAPI_VERSION}
  location = "https://cam-kp-api.renci.org"
  location = ${?LOCATION}
  sparql-endpoint = "https://cam-kp-sparql.apps.renci.org/sparql"
  sparql-endpoint = ${?SPARQL_ENDPOINT}
  maturity = "development"
  maturity = ${?MATURITY}
}
